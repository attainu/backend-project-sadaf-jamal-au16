<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
        <img src="/images/navbar-image/burger.png" alt="" width="30" height="24" class="d-inline-block align-text-top">
        Hungry
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 ">
        <li class="nav-item">
          <a class="nav-link active" href="/seller/dashboard">Partner dashboard</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/orders">Orders</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/logout">Logout</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div>
  <h1>{{restaurantName}}</h1>
  <p><strong>Email:</strong></p>
  <p><strong>{{email}}</strong></p>
  <p><strong>Address:</strong></p>
  <p><strong>{{address}}</b></p>
</div>

<div class="d-flex">
    {{#if items}}
      {{#each items}}
      <div>
        {{this.itemName}}
        {{this.price}}
      </div>
      {{/each}}
    {{else}}
      <div>no items listed for your restaurant</div>
    {{/if}}
</div>