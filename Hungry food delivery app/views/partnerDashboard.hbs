<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <img src="/images/navbar-image/burger.png" alt="logo" class="d-inline-block align-text-top" width="30"
        height="24">
      Hungry
    </a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 ">
        <li class="nav-item">
          <a class="nav-link" href="#">Partner Dashboard</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/partner/orders?_id={{this._id}}">Orders</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active logout" href="#">Logout</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="card mb-3 w-100">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{imageUrl}}" alt="restaurant image" class="w-100">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">{{restaurantName}}</h5>
        <p class="card-text">Email: {{email}}</p>
        <p class="card-text">Address: {{address}}</p>
        <p class="card-text"><small class="text-muted">Note: Hungry holds all Rights to remove partner in case of misuse
            of platform</small></p>
      </div>
    </div>
  </div>
</div>

<div class="row row-cols-1 row-cols-md-5 g-2 mx-1 mb-5">
  {{#each items}}
  <div class="col">
    <div class="card h-100 w-100" style="width: 18rem;">
      <img src="{{this.imageUrl}}" class="card-img-top" alt="...">
      <div class="card-body">
        <h5 class="card-title">{{this.title}}</h5>
        <p class="card-text"><strong>Description: </strong>{{this.description}}</p>
        <p class="card-text"><strong>Price: </strong>{{this.price}}</p>
        <a href="/partner/item/edit/{{this._id}}" class="btn btn-success">Edit Item</a>
        <a href="/partner/item/delete/{{this._id}}" class="btn btn-danger">Delete Item</a>
      </div>
    </div>
  </div>
  {{else}}
  <p class="text-center w-100 fs-4 fw-bolder">no items listed for your restaurant</p>
  {{/each}}
</div>

<div class="d-flex justify-content-center">

  <a href="/partner/item?_id={{this._id}}" class="btn btn-success btn-lg" tabindex="-1" role="button">Add Item</a>
</div>

<script>
  const logOutBtn = document.querySelectorAll('.logout')[0]

  logOutBtn.addEventListener('click', () => {

    var res = document.cookie;
    var multiple = res.split(";");
    for (var i = 0; i < multiple.length; i++) {
      var key = multiple[i].split("=");
      document.cookie = key[0] + " =; expires = Thu, 01 Jan 1970 00:00:00 UTC";
    }
    window.location.replace("/")

  })
</script>